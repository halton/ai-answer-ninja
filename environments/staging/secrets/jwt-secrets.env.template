# Staging JWT Secrets Template
# Copy this to jwt-secrets.env and replace with actual staging secrets
# Generate secure random keys for production use

# JWT Secrets (Staging) - Generate with: openssl rand -base64 64
JWT_ACCESS_SECRET=REPLACE_WITH_STAGING_JWT_ACCESS_SECRET_64_CHARS_MIN
JWT_REFRESH_SECRET=REPLACE_WITH_STAGING_JWT_REFRESH_SECRET_64_CHARS_MIN

# Session Secrets - Generate with: openssl rand -base64 64
SESSION_SECRET=REPLACE_WITH_STAGING_SESSION_SECRET_64_CHARS_MIN

# Encryption Keys - Generate with: openssl rand -base64 32
ENCRYPTION_KEY=REPLACE_WITH_STAGING_ENCRYPTION_KEY_32_BYTES
ENCRYPTION_IV=REPLACE_WITH_STAGING_ENCRYPTION_IV_16_BYTES

# API Keys for Internal Services
INTERNAL_API_KEY=REPLACE_WITH_STAGING_INTERNAL_API_KEY
SERVICE_MESH_TOKEN=REPLACE_WITH_STAGING_SERVICE_MESH_TOKEN

# Redis Authentication (Staging)
REDIS_PASSWORD=REPLACE_WITH_STAGING_REDIS_PASSWORD

# Cookie Secrets - Generate with: openssl rand -base64 64
COOKIE_SECRET=REPLACE_WITH_STAGING_COOKIE_SECRET_64_CHARS_MIN

# Database Encryption Key
DB_ENCRYPTION_KEY=REPLACE_WITH_DB_ENCRYPTION_KEY_32_BYTES

# File Upload Encryption
FILE_ENCRYPTION_KEY=REPLACE_WITH_FILE_ENCRYPTION_KEY_32_BYTES

# MFA Secret Key
MFA_SECRET_KEY=REPLACE_WITH_MFA_SECRET_KEY

# OAuth Secrets
OAUTH_GOOGLE_CLIENT_SECRET=REPLACE_WITH_GOOGLE_OAUTH_SECRET
OAUTH_GITHUB_CLIENT_SECRET=REPLACE_WITH_GITHUB_OAUTH_SECRET