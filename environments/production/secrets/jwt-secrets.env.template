# Production JWT Secrets Template
# Copy this to jwt-secrets.env and replace with actual production secrets
# Generate secure random keys for production use
# NEVER commit actual production secrets to version control

# JWT Secrets (Production) - Generate with: openssl rand -base64 64
JWT_ACCESS_SECRET=REPLACE_WITH_PRODUCTION_JWT_ACCESS_SECRET_64_CHARS_MIN
JWT_REFRESH_SECRET=REPLACE_WITH_PRODUCTION_JWT_REFRESH_SECRET_64_CHARS_MIN

# Session Secrets - Generate with: openssl rand -base64 64
SESSION_SECRET=REPLACE_WITH_PRODUCTION_SESSION_SECRET_64_CHARS_MIN

# Encryption Keys - Generate with: openssl rand -base64 32
ENCRYPTION_KEY=REPLACE_WITH_PRODUCTION_ENCRYPTION_KEY_32_BYTES
ENCRYPTION_IV=REPLACE_WITH_PRODUCTION_ENCRYPTION_IV_16_BYTES

# API Keys for Internal Services
INTERNAL_API_KEY=REPLACE_WITH_PRODUCTION_INTERNAL_API_KEY
SERVICE_MESH_TOKEN=REPLACE_WITH_PRODUCTION_SERVICE_MESH_TOKEN

# Redis Authentication (Production)
REDIS_PASSWORD=REPLACE_WITH_PRODUCTION_REDIS_PASSWORD
REDIS_CLUSTER_PASSWORD=REPLACE_WITH_REDIS_CLUSTER_PASSWORD

# Cookie Secrets - Generate with: openssl rand -base64 64
COOKIE_SECRET=REPLACE_WITH_PRODUCTION_COOKIE_SECRET_64_CHARS_MIN

# Database Encryption Key - Generate with: openssl rand -base64 32
DB_ENCRYPTION_KEY=REPLACE_WITH_PRODUCTION_DB_ENCRYPTION_KEY_32_BYTES

# File Upload Encryption - Generate with: openssl rand -base64 32
FILE_ENCRYPTION_KEY=REPLACE_WITH_PRODUCTION_FILE_ENCRYPTION_KEY_32_BYTES

# MFA Secret Key - Generate with: openssl rand -base64 32
MFA_SECRET_KEY=REPLACE_WITH_PRODUCTION_MFA_SECRET_KEY

# OAuth Secrets (Production)
OAUTH_GOOGLE_CLIENT_SECRET=REPLACE_WITH_GOOGLE_OAUTH_PROD_SECRET
OAUTH_GITHUB_CLIENT_SECRET=REPLACE_WITH_GITHUB_OAUTH_PROD_SECRET
OAUTH_MICROSOFT_CLIENT_SECRET=REPLACE_WITH_MICROSOFT_OAUTH_PROD_SECRET

# SAML Secrets
SAML_CERTIFICATE=REPLACE_WITH_SAML_CERTIFICATE
SAML_PRIVATE_KEY=REPLACE_WITH_SAML_PRIVATE_KEY

# Webhook Secrets
WEBHOOK_SECRET_KEY=REPLACE_WITH_WEBHOOK_SECRET_KEY

# Backup Encryption Keys
BACKUP_ENCRYPTION_KEY=REPLACE_WITH_BACKUP_ENCRYPTION_KEY
ARCHIVE_ENCRYPTION_KEY=REPLACE_WITH_ARCHIVE_ENCRYPTION_KEY

# Certificate Private Keys (if not using cert-manager)
TLS_PRIVATE_KEY=REPLACE_WITH_TLS_PRIVATE_KEY
TLS_CERTIFICATE=REPLACE_WITH_TLS_CERTIFICATE

# Service-to-Service Authentication
SERVICE_AUTH_TOKEN=REPLACE_WITH_SERVICE_AUTH_TOKEN
MICROSERVICE_SHARED_SECRET=REPLACE_WITH_MICROSERVICE_SHARED_SECRET